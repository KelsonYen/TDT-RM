# TDT-RM Production Pipeline Audit Report

- **Audit date:** 2026-06-04 UTC
- **Requested trade date:** 2026-06-03
- **Scope:** Validate whether the production pipeline can automatically fetch all official-source-first datasets, create production-valid daily inputs, run TDT-RM final daily output generation, and emit the final report.
- **Model-rule status:** No model rules, scoring logic, ETI/TCWRS/CAL rules, or decision-matrix logic were modified during this audit.

## Executive conclusion

The production pipeline was executed for trade date `2026-06-03`, but this environment did **not** complete a production-valid run. The pipeline failed closed during the live provider-fetch stage before production input materialization and before final report generation for this audit run.

The blocking condition was environment connectivity: all official/public provider HTTPS attempts returned proxy tunnel failures (`Tunnel connection failed: 403 Forbidden`). FinMind was not enabled because no `FINMIND_TOKEN` secret was present and the workflow only permits it as an explicit token-gated vendor fallback.

Therefore, the audit result is:

- **Production-valid run:** `NO`
- **Official-source auto-fetch completed:** `NO`
- **Strict production input directory materialized by this run:** `NO`
- **Final TDT-RM production report generated by this run:** `NO`
- **Fail-closed behavior verified:** `YES`

## Commands executed

### GitHub Actions install step simulation

```bash
python -m pip install --upgrade pip
if [[ -f requirements.txt ]]; then python -m pip install -r requirements.txt; fi
python -m pip install -e .
```

Result: failed before editable install because this runtime could not fetch build dependencies through the configured HTTPS proxy:

```text
ERROR: Could not find a version that satisfies the requirement setuptools>=68 (from versions: none)
ERROR: No matching distribution found for setuptools>=68
```

This is an environment dependency/network limitation, not a model-rule or scoring failure.

### Production pipeline entrypoint

```bash
GITHUB_ACTIONS=true CI=true TDT_RM_PRODUCTION_MODE=true \
  python scripts/run_github_actions_production_fetch.py \
    --trade-date 2026-06-03 \
    --source-config config/public_data_sources.json
```

Result: failed closed during `scripts/fetch_daily_data_multi_provider.py` before production input materialization:

```text
ERROR GitHub Actions production fetch failed closed: production provider fetchers failed with exit code 1
```

## Provider-fetch result

The generated provider summary reported `overall_status: NOT_READY`, with all required datasets missing from this run:

- `price.csv`
- `foreign_flow.csv`
- `fx.csv`
- `breadth.csv`
- `futures.csv`
- `options.csv`
- `leadership.csv`

Provider health summary:

```json
{
  "fail_closed": true,
  "failed_providers": [
    "breadth_provider",
    "foreign_flow_provider",
    "futures_provider",
    "fx_provider",
    "leadership_provider",
    "options_provider",
    "price_provider"
  ],
  "healthy_providers": [],
  "total_providers": 7,
  "validation_failed": true
}
```

Primary blocking reason by provider group:

| Dataset | Official/public providers attempted | Outcome |
| --- | --- | --- |
| `price` | TWSE official, Taiwan Index Plus, Yahoo Finance, Stooq | TWSE/Yahoo/Stooq HTTPS proxy tunnel failed; Taiwan Index Plus source config missing; FinMind disabled. |
| `foreign_flow` | TWSE official | TWSE HTTPS proxy tunnel failed; FinMind disabled. |
| `fx` | TAIFEX official, CBC official, Yahoo Finance | TAIFEX/CBC/Yahoo HTTPS proxy tunnel failed; FinMind disabled. |
| `breadth` | TWSE official | TWSE HTTPS proxy tunnel failed; FinMind disabled. |
| `futures` | TAIFEX official | TAIFEX HTTPS proxy tunnel failed; FinMind disabled. |
| `options` | TAIFEX official | TAIFEX HTTPS proxy tunnel failed; FinMind disabled. |
| `leadership` | TWSE official, Yahoo Finance | TWSE/Yahoo HTTPS proxy tunnel failed; FinMind disabled. |

## Artifact inventory from this audit run

The failed-closed fetch stage wrote diagnostic artifacts under:

- `reports/daily/2026-06-03/artifacts/production_fetch_summary.json`
- `reports/daily/2026-06-03/artifacts/provider_health.json`

Because the pipeline failed at provider acquisition, this audit run did not refresh or prove the existing dated report files that were already present in the repository before this audit.

## Production-readiness determination

A production-valid run requires all required official-source-first datasets to be fetched, strict CSV validation to pass, production input files under `inputs/daily/2026-06-03/` to be materialized, and final daily report artifacts to be generated from those fresh inputs.

This audit did not satisfy those criteria. The correct production status for `2026-06-03` in this runtime is therefore **BLOCKED / NOT_READY**.

## Recommended next action

Run the same workflow on a real GitHub-hosted Actions runner with normal outbound HTTPS connectivity and, if vendor fallback is desired, configure `FINMIND_TOKEN` as a GitHub Actions secret. Treat any current local/Codex run as connectivity-blocked until a green GitHub Actions run produces the required input and report artifacts.
